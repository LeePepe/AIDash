#if os(macOS)
import AppKit

/// Controls the macOS menu bar status item for AIDash.
/// TODO(T081): Full menu bar implementation with status item, menu, and popover.
@MainActor
final class MenuBarController {
    init() {
        // TODO(T081): Create NSStatusItem and configure menu
    }
}
#endif

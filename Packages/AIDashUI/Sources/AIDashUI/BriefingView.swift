import SwiftUI

/// The main briefing view showing today's cards.
/// Full implementation arrives in T090; this is the compilation stub.
public struct BriefingView: View {
    public init() {}

    public var body: some View {
        Text("BriefingView placeholder")
    }
}
